name: server-diagnose
on: { workflow_dispatch: {} }
jobs:
  containers:
    runs-on: self-hosted
    steps:
      - name: All trading-bot containers and status
        run: docker ps -a --filter "name=trading-bot" --format 'table {{.Names}}\t{{.Status}}' | sort
