# Backtest

Not implemented yet. This folder is a placeholder for a future backtesting engine that
replays cached `candles` rows through `Strategy.DetectCrossoverSignal` and
`PositionSizer.Calculate` without touching the live/demo exchange.
